{
  "name": "my-backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p build && cp Index.html build/index.html && cp API_DOCUMENTATION.md build/ 2>/dev/null || true"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase": "^9.22.0"
  }
}
