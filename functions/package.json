{
  "name": "connect-yacht-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint . || true",
    "build": "echo \"No build step for functions\"",
    "start": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.3.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0",
    "supertest": "^6.3.0"
  }
}
