rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Rule for vessels collection and all nested subcollections
    // Allows authenticated users to perform all read and write operations
    match /vessels/{vesselId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
