version: 1
app:
  name: yachtmanagement
  region: us-east4
deploy:
  hosting:
    - target: app
runtime:
  env:
    NODE_ENV: production
    # Add other runtime envs in the Studio UI (do NOT put secrets here in source)
    # STRIPE_KEY: "pk_test_xxx"
build:
  command: "npm run build"
